type User {
    userId: ID!
    nickname: String!
    email: String!
    status: Status!
    createdAt: String!
    updatedAt: String!
}

enum Status {
    COMMON
    MEMBERSHIP1
    MEMBERSHIP2
    ADMIN
}