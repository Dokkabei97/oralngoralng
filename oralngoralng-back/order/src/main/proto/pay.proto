syntax = "proto3";

service PayService {
  rpc registerPayRequest(payment) returns (payment);
}

message payment {
  int64 userId = 1;
  int64 productId = 2;
  int64 price = 3;
  PayMethod payMethod = 4;

}

enum PayMethod {
  CARD = 0;
  NAVER_PAY = 1;
  TOSS_PAY = 2;
  KAKAO_PAY = 3;
}